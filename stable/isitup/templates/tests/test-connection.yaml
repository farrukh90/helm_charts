apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "istiup.fullname" . }}-test-connection"
  labels:
{{ include "istiup.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "istiup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
